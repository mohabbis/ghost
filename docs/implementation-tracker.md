# Organizer trust implementation tracker

| Finding ID | Severity | Description | Source file and line | Planned fix | Branch | PR | Regression test | CI result | Merge SHA | Closure evidence | Remaining limitations |
|---|---|---|---|---|---|---|---|---|---|---|---|
| GHO-CRIT-001 | Critical | Undo journal could describe intended mutations because entries were committed before filesystem success/postcondition verification. | `src-tauri/src/organizer/executor.rs` | Commit undo entries only after successful mutation verification. | `master` | #169 (earlier) | `failed_folder_creation_does_not_commit_undo`; `failed_move_or_rename_does_not_commit_undo`; `mixed_success_and_failure_journals_only_successful_mutations` | Green | merged | Executor prepares inverse ops before mutation and records them only after verified success (`undo.record` after postcondition check). | None for this specific finding. |
| GHO-CRIT-002 | Critical | Undo journal was held in memory until run end — a crash mid-run lost recovery data for already-applied steps. | `src-tauri/src/commands/organizer.rs` (pre-fix), `storage/executions.rs` | Write-ahead durability: persist execution + undo journal after every action; surface unfinished runs on next launch. | `master` | #181 | Mid-run crash simulation test in `storage/executions.rs`; WAL chain-predecessor test | Green | `6f942ef` / `064eb60` | `begin_execution` / `update_execution_progress` / `finish_execution`; v6 `finished` column; `organizer_check_unfinished_run` + UI banner | Dismiss-without-undo is intentional; pre-v6 runs default `finished = 1` and are not recoverable as interrupted. |
| GHO-HIGH-003 | High | Updater public key is still a placeholder — auto-update cannot verify signatures in production. | `src-tauri/tauri.conf.json` `plugins.updater.pubkey` | Generate keypair, embed pubkey, configure `TAURI_SIGNING_PRIVATE_KEY` in release workflow. | — | — | Updater no-op when pubkey missing (graceful degrade) | — | — | Commands fail closed / no-op cleanly when pubkey is placeholder | **Open.** Requires maintainer action; see `RELEASING.md` and `docs/auto-update.md`. |
| GHO-HIGH-004 | High | Public version/download channel may lag repo version. | README, site, GitHub Releases | Publish `v1.2.7` tag + assets or align marketing copy to actual latest release. | — | — | — | — | — | Release workflow publishes atomically when tag is pushed | **Open.** Verify `https://github.com/mohabbis/ghost/releases` before claiming a download version. |
