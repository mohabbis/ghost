# Pin the toolchain so every contributor, CI runner, and AI agent working in
# parallel compiles with the same stable compiler and has rustfmt + clippy
# available. Matches CI (dtolnay/rust-toolchain@stable).
[toolchain]
channel = "stable"
components = ["rustfmt", "clippy"]
