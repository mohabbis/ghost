//! Fabric API connector (planned — read/export only in phase one).

pub struct FabricClient;

impl Default for FabricClient {
    fn default() -> Self {
        FabricClient
    }
}

impl FabricClient {
    pub fn new() -> Self {
        Self
    }
}
