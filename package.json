{
  "name": "ghost-desktop-legacy",
  "description": "Legacy Rust/Tauri desktop app (superseded). The active product is the cloud SaaS in cloud/ — see CONTRIBUTING.md. Installing here does not get you Ghost Cloud.",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:execution": "tsc -p tsconfig.execution.json",
    "build": "npm run build:execution && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.execution.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.1.5"
  }
}
